1641.Count Sorted Vowel Strings
dp
public int countVowelStrings(int n) {
    int[] dp = new int[5];
    dp[0] = 1;
    for (int i = 0; i <= n; i++) {
        for (int j = 1; j < 5; j++) {
            dp[j] += dp[j - 1];
        }
    }
    return dp[4];
}